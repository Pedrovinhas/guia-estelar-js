// Object

const person = {
    name: 'John',
    age: 30,
    weight: 88.6,
    isAdmin: true

}

// Imprime no console o objeto completo
console.log(person)

// Imprime a propriedade escolhida no console
console.log(person.age)

// Interpolação de propriedade de objetos no console
console.log(`${person.name} tem ${person.age}`)